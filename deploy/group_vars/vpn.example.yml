# Optional public overrides. The defaults match the current production backend.
vpn_backend_url: https://backend.example.invalid
vpn_reality_target: example.com:443
vpn_reality_server_name: example.com
vpn_certbot_email: admin@example.invalid

# The playbook derives the public IPv4 from ansible_host and issues a certificate
# for <dash-separated-ip>.sslip.io. Override only when using another hostname.
# vpn_public_ipv4: 192.0.2.10
# vpn_hysteria_domain: 192-0-2-10.sslip.io
